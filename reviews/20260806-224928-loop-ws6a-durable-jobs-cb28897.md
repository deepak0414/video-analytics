# Agent review — approve

date: 2026-08-06T22:52:20.731849
range: origin/main..HEAD
branch: loop/ws6a-durable-jobs
findings: 0


---

## Full review

Two pytest runs are live — per the 2026-08-04 lesson I won't launch another; I verified the delta by inspection instead. All checks are complete; here is the review.

# Review — origin/main..HEAD (`cb28897`, WS6.a durable job queue, round 9)

## Verdict: approve, no findings

The delta since round 8's approved code (`7eff910`) is exactly the two pinning tests that round's single minor finding requested — `git diff 7eff910..HEAD` touches only `tests/test_jobs_durable.py` (+74 lines). Both tests are correct and genuinely pin the fix paths:

**`test_requeue_if_running_never_reverts_terminal_states`** directly unit-tests `JobStore.requeue_if_running`'s guard: a `done` row with a result survives the requeue untouched (state and result both asserted), and the positive case (a `running` row does revert to `queued`) is covered in the same test. This closes the round-8 gap where the guard's distinguishing behavior was only indirectly exercised.

**`test_resume_nulls_prior_appearance_refs_when_tracker_fails`** is the harder one, and I verified its premises hold:

- The rig is a faithful copy of the established `test_appearance_store.py` pattern — same `{"roles": {...}}` footage-profile shape (matches `configuration.py`'s `_load_footage_overlay`, which reads `doc.get("roles")`), same `VA_CONFIG_DIR` monkeypatch set before the first ingest, same `write_box_video` signature (checked against `media/synth.py:19`).
- The monkeypatched `get_object_tracker` raise lands inside the best-effort try at `src/va/pipeline/ingest.py:632-633`, so the resumed ingest completes rather than aborting, `replace_tracks` never runs, and the prior-attempt track rows survive — exactly the dangling-ref scenario the fix nulls.
- It fails on the pre-fix code: `refs_before >= 1` is asserted before the forged mid-kill state, so without the nulling UPDATE the final `dangling == 0` assertion would see those surviving refs. It is a real regression tripwire for logic that otherwise lives inside a swallow-all except.
- The cap arithmetic in the poison test still lines up (`bump` then `> MAX_RESUME_ATTEMPTS` allows exactly 3 resumes; the test seeds 3 prior bumps so the next restart is one too many).

## Verified and dissolved (not findings)

- **Post-stop drain window:** I chased whether a graceful stop could accrue crash-guilt on the *next* queued job (worker drains the queue after `stop()` returns, process death mid-job leaves `running`). The common case is safe: the in-flight job at stop time is requeued guilt-free by `requeue_if_running`, and a process that dies while that same job is still running leaves it `queued` (no bump on resume). The remaining window — worker finishes job A and starts job B between join-timeout and process exit, three separate times for the same job — is too narrow and too self-announcing (pollable "gave up" error, resubmittable) to report.
- **Degraded-store cap bypass:** if the jobs table is broken, `bump_attempts` returns None and the cap check is skipped — intentional best-effort doctrine (degraded mode has no durable state to loop on anyway).
- The unclosed sqlite connection on the nulling's exception path and the pre-existing stop/start re-entrancy window were examined and declined in prior rounds; nothing in this delta changes that judgment.
- Docs parity (CLAUDE.md restart semantics, COORDINATION.md ⚠ cross-boundary entry with the round-4/5 amendments), combination coverage (queue/table layer is config-orthogonal; the new test exercises the stub detector/tracker path the fix actually runs under), and test integrity (delta is purely additive) all hold as in round 8. The `need_agent_review:` subject is a provisional artifact, exempt from the clarity rule.

```json
{"verdict": "approve", "findings": []}
```
