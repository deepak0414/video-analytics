# Agent review — approve

date: 2026-08-07T09:34:01.633874
range: origin/main..HEAD
branch: loop/r11a-scan-target
findings: 0


---

## Full review

Review complete. This is round 3 for R11.a; the HEAD commit (ab8f875) is the rework addressing round 2's two minor findings. A pytest suite is live in another session, so per the 2026-08-04 lesson I did not launch a run; verification was static plus a live probe of `derive_scan_target` (not pytest — a single in-process function call).

**Both round-2 findings are resolved in this commit:**

1. **Pronoun-only subject-free sweep** (round-2 minor #1): fixed exactly as the review prescribed. Stage 2 now requires at least one subject word among the survivors — outside `_SCAN_NOISE`, or in {color, colour, number} (the subject nouns `_STOP` absorbs for search purposes). I probed the function live: `"how many times did it change?"` → `None` (was `"the times change"`), `"how many times did she change?"` → `None`, while `"…does the color change…"` → `"the times color change video"` and `"…did she change her dress?"` → `"the dress"` still derive correctly. The new test line pins the pronoun-only case. The `w not in _SCAN_NOISE` disjunct in the guard is unreachable by construction (stage 2 only runs when every word is in `_SCAN_NOISE`), but it's defensive, not wrong — no finding.

2. **Golden-ask Done-when evidence** (round-2 minor #2): the COORDINATION.md entry now records an actual harness run on this change (dresses-ask-01 passed, 330 s; bird-ask-01 passed on retry after a claude-CLI subprocess-timeout flake, honestly reported as such). The R11.a Done-when — "security profile → outfit target can no longer fire; A-EV golden-ask harness still green" — is now met on both halves.

**What I re-verified independently this round:**

- The profile gate: `config_for(video.profile, video.source_type.value)` matches the signature in `configuration.py:145`; `FootageSettings.deep_scan` is `Literal["auto", "off"]` so the `== "off"` comparison is well-typed; all four config dirs' `security.yaml` carry `deep_scan: "off"`; a NULL `profile` falls back to the source-derived default. A missing/broken profile at gate time would raise inside `_deep_scan_into`'s catch and degrade to a note — best-effort is preserved.
- The `run_deep_scan` return-contract change `(result, skip_reason)`: all three skip paths return a reason, `deep_scan_video` always returns a result or raises (so `(None, None)` is unreachable), the only callers are `ask.py` and the tests (both updated), no stale assertions on the old note string anywhere, and the change is logged in COORDINATION.md.
- `DEFAULT_TARGET` is gone from `src/` entirely (remaining mentions are historical notes files and prior reviews).
- Deep-scan cache keys are unchanged for stage-1-derivable queries (the derivation is byte-identical to the old inline code), consistent with the recorded green golden run.
- Tests: only additions; the new paths (profile gate end-to-end with cause-specific note, no-target skip, derivation edge cases) are each pinned.

Deliberate tradeoffs I probed and chose not to report: `"how many scene changes?"` derives `None` (scene/change are counting-noise by design — Role-1 segments answer that question, and a sweep for "scene" would be the camera-cut-counting failure CLAUDE.md's determinism lesson records); the {color, colour, number} whitelist is hardcoded structure mirroring `_STOP`'s composition, explicitly flagged in comments and vetted across two review rounds, not silent content.

```json
{"verdict": "approve", "findings": []}
```
