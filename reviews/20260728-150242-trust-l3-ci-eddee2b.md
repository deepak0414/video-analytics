# Agent review — request_changes

date: 2026-07-28T15:09:10.878093
range: origin/main...eddee2b8b04557d5b474554710d25b86abfed8d8
branch: trust/l3-ci
findings: 3

- **critical** `tests/test_migrations.py:11` — Committed test imports MIGRATIONS/SCHEMA_VERSION/add_column from va.storage.structured.schema, but schema.py is unchanged in the range — the migration runner exists only as an uncommitted working-tree edit.
  - scenario: Any clean checkout of eddee2b (including the offline-tests CI job this branch introduces) fails pytest collection with ImportError, turning the whole offline suite red and making the branch's own required check permanently fail on its own PR; /verify cannot produce a green evidence block.
- **minor** `.claude/hooks/bash_guard.py:336` — The credential-extraction block matches only `gh auth token`; `gh auth status --show-token` (or -t) prints the same token and passes all rules.
  - scenario: An agent runs `gh auth status --show-token`, obtains the human's credential, and proceeds to the same curl label-application path the round-4 fix was written to slow down — the newly added speed bump is bypassed by its most obvious sibling spelling.
- **minor** `workflow-trust-plan.md:1923` — The round-4 as-built note claims the CLAUDE.md overclaim is corrected and WT.7's deliverable includes a CLAUDE.md note, but CLAUDE.md is untouched in the range — the 13-line note is only an uncommitted working-tree edit.
  - scenario: The branch merges as committed; COORDINATION.md announces the label contract as live while CLAUDE.md never documents it, and the plan's as-built record asserts a correction that is not in the repo's history.

---

## Full review

## Review: `origin/main...eddee2b` (WT.5–WT.7 CI gates, backstop rounds 1–4)

**Verdict: request_changes** — one critical, two minor.

First, credit where verified: all four findings from the round-4 ledger (`reviews/20260728-144209-…`) are genuinely fixed in eddee2b — HTTP clients and `gh auth token` are now blocked with the D9 honesty note, the evidence check requires real pytest counts (which also fixes the ledger's inverse false positive), `critical-paths` checks out the PR head instead of the merge ref, and `trust_repo` pops `VA_AGENT_REVIEW`. I traced the guard's new gh parsing (`gh_words`, the mutating/body-from-file regexes) against every new BLOCKED/ALLOWED test string by hand and found no logic error; the checker's pure-bash prefix match, rename handling (`-M --name-status`, both paths), and fail-closed paths are correct as written.

**Critical — the branch ships tests for code that is not on the branch** (`tests/test_migrations.py:11`). The file, added in eddee2b, does `from va.storage.structured.schema import MIGRATIONS, SCHEMA_VERSION, add_column, connect` — but `schema.py` is not in the commit range and the committed copy contains none of those names (verified: `git show eddee2b:src/va/storage/structured/schema.py` has zero occurrences; `connect` exists but the migration runner does not). The runner exists only as an **uncommitted working-tree edit** in this session (`git status` shows `schema.py` modified). Failure scenario: any clean checkout of eddee2b — including the GitHub runner this very branch creates — hits an ImportError at collection, so `pytest -q` errors and the `offline-tests` required check the branch introduces is red on its own PR; `/verify` cannot emit green evidence either. The updated comment in `tests/test_trace_link.py:119` ("apply_schema runs the migration runner") describes the same nonexistent-in-range code. The fix is simply to commit the schema.py migration runner with (or before) its tests.

**Minor — `gh auth status --show-token` defeats the new `gh auth token` block** (`.claude/hooks/bash_guard.py:336`). The block matches only `auth token` at words 0–1; `gh auth status --show-token` (or `-t`) prints the same credential and passes every rule (noun `auth`, action `status`, no label mention). D9 already concedes this whole layer is a speed bump, so this is not a hole in the guarantee — but it is the obvious sibling of the exact spelling the round-4 fix chose to block, and the same one-line rule covers it.

**Minor — the plan claims a CLAUDE.md correction the range doesn't contain** (`workflow-trust-plan.md:1923`). The round-4 as-built note states "the overclaim in CLAUDE.md/COORDINATION.md is corrected", and WT.7's deliverable includes "a CLAUDE.md note" — but CLAUDE.md is identical to origin/main across the range; the 13-line note exists only as another uncommitted working-tree edit. Same root cause as the critical finding: the session's fixes were only partially committed. Scenario: the branch merges as-is and the other agent's CLAUDE.md never documents the label contract while COORDINATION.md announces it as live.

```json
{"verdict": "request_changes", "findings": [
{"severity": "critical", "file": "tests/test_migrations.py", "line": 11, "issue": "Committed test imports MIGRATIONS/SCHEMA_VERSION/add_column from va.storage.structured.schema, but schema.py is unchanged in the range — the migration runner exists only as an uncommitted working-tree edit.", "scenario": "Any clean checkout of eddee2b (including the offline-tests CI job this branch introduces) fails pytest collection with ImportError, turning the whole offline suite red and making the branch's own required check permanently fail on its own PR; /verify cannot produce a green evidence block."},
{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 336, "issue": "The credential-extraction block matches only `gh auth token`; `gh auth status --show-token` (or -t) prints the same token and passes all rules.", "scenario": "An agent runs `gh auth status --show-token`, obtains the human's credential, and proceeds to the same curl label-application path the round-4 fix was written to slow down — the newly added speed bump is bypassed by its most obvious sibling spelling."},
{"severity": "minor", "file": "workflow-trust-plan.md", "line": 1923, "issue": "The round-4 as-built note claims the CLAUDE.md overclaim is corrected and WT.7's deliverable includes a CLAUDE.md note, but CLAUDE.md is untouched in the range — the 13-line note is only an uncommitted working-tree edit.", "scenario": "The branch merges as committed; COORDINATION.md announces the label contract as live while CLAUDE.md never documents it, and the plan's as-built record asserts a correction that is not in the repo's history."}
]}
```
