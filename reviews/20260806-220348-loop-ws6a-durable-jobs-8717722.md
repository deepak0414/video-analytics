# Agent review — approve

date: 2026-08-06T22:09:13.239331
range: origin/main..HEAD
branch: loop/ws6a-durable-jobs
findings: 1

- **minor** `src/va/web/jobs.py:181` — The round-2 fix making _resume skip a malformed-but-parseable jobs row (try/except per row in IngestQueue._resume and AskQueue._resume) has no covering test — the 'never a dead queue' promise is pinned only for the broken-store path, not the bad-row path.
  - scenario: A later refactor hoists the IngestJob/AskJob construction out of the per-row try block; a jobs row with payload '{}' then raises KeyError inside start() during the FastAPI lifespan and `va serve` fails to boot, with the whole suite still green. Add a test per queue that seeds a malformed row next to a valid one and asserts startup succeeds and the valid job completes (it fails on the pre-fix code, pinning the guard).

---

## Full review

Review complete. This is round 3 of WS6.a, and the substantive holes from the earlier rounds are genuinely closed — I re-derived each rather than trusting the prior reviews:

**Prior findings, re-verified as fixed at HEAD:**
- Round 1's major (resume re-running `ingest()` against a persisted partial shard, doubling every frame/appearance vector via the load-and-append `NumpyFlatVectorStore`) is fixed by the unlink at `src/va/pipeline/ingest.py:504-506`. I confirmed the premise and placement: the `done` dedup return (`ingest.py:232`) precedes it, so any row reaching the embed stage is a partial attempt; the filenames match `_vec_file`/`_payload_file` exactly; the frame store and `_capture_appearance`'s store are both constructed after the unlink; `index_text` already rebuilds via temp+`swap_shard` so the text shard needs no cleanup; role tables all use `replace_*`/purge; `va reingest` rmtree's the whole video dir first so no conflict; and keyframes dirs are written by `ask`, not ingest. The new `test_resume_after_kill_with_partial_shard_does_not_duplicate_vectors` forges exactly the mid-kill state (persisted shard + `processing` row + `running` job) and asserts the data-level count, satisfying both the Done-when ("idempotency asserted") and the fail-on-old-code lesson.
- Round 2's KeyError-kills-startup minor is fixed: both `_resume` rebuild loops now wrap each row in try/except-log-continue (`jobs.py:181-190`, `259-267`).
- Round 2's COORDINATION overstatement is fixed: the entry now states precisely that done/failed history rows persist but `get()` reads memory only.

**Things I checked that dissolved:** schema v7 follows the migration recipe exactly (base DDL + idempotent `CREATE IF NOT EXISTS` migration + version bump; the `MIGRATIONS`-count assert holds; older-build-opens-v7 hits the additive-schema warning path); `_persist` opens a fresh per-call connection so no cross-thread SQLite sharing, and `connect()`'s WAL + 60s busy timeout covers contention; the resume-test monkeypatch intercepts because `_process` imports `ingest` at call time; `AskQueue`'s failed rebuilds are never enqueued, so the never-re-run assertion is sound; profile carry-forward on resume works (`videos.profile` is set before fetch and carried at `ingest.py:223-230`); `va serve` runs a single-process uvicorn with an app object, so `_resume()` runs once — no multi-worker double-resume; the public `submit()`/`get()`/`to_dict` surface and `app.py` are untouched; unlink-then-rebuild respects the .json-before-.npz torn-pair invariant for concurrent readers; no new env vars or CLI flags exist, and CLAUDE.md + COORDINATION.md carry the new semantics; the provisional `need_agent_review:` subject is exempt from the clarity rule. A pytest run was already live, so per the repo lesson I did not launch the suite; verification of the new tests is static.

**One minor remains:**

1. **Minor — `src/va/web/jobs.py:181` (ask twin at 259): the round-2 "one bad row costs one job, not the server" fix has no covering test.** The degraded-mode promise is pinned for a broken store (`test_no_workdir_store_failure_degrades_to_memory`) but not for a malformed-but-parseable row: nothing seeds a jobs row with payload `{}` and asserts the server still boots and other jobs still resume. A refactor that hoists the `IngestJob(...)` construction out of the per-row try (say, into a comprehension) reintroduces the boot-killing KeyError silently, and the suite stays green. Safe path: add one test per queue seeding a `{"kind": "ingest", "payload": "{}"}` row alongside a valid one, asserting startup succeeds and the valid job completes — written against the pre-fix code it fails, so it actually pins the fix.

No critical or major findings, so this passes.

```json
{"verdict": "approve", "findings": [
  {"severity": "minor", "file": "src/va/web/jobs.py", "line": 181,
   "issue": "The round-2 fix making _resume skip a malformed-but-parseable jobs row (try/except per row in IngestQueue._resume and AskQueue._resume) has no covering test — the 'never a dead queue' promise is pinned only for the broken-store path, not the bad-row path.",
   "scenario": "A later refactor hoists the IngestJob/AskJob construction out of the per-row try block; a jobs row with payload '{}' then raises KeyError inside start() during the FastAPI lifespan and `va serve` fails to boot, with the whole suite still green. Add a test per queue that seeds a malformed row next to a valid one and asserts startup succeeds and the valid job completes (it fails on the pre-fix code, pinning the guard)."}
]}
```
