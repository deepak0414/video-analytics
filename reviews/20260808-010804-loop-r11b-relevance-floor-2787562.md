# Agent review — approve

date: 2026-08-08T01:14:39.285457
range: origin/main..HEAD
branch: loop/r11b-relevance-floor
findings: 1

- **minor** `video-analytics-model-analysis.md:94` — The decision block recording the FLAGGED min_cosine: 0.0 uses two unexplained denominators for "ground-truth clips" (6/26 from the 9-query retrieval measurement, 7/25 from the 8-question end-to-end run), so the figures read as mutually contradictory — 7 in the final evidence appears to exceed the 6 that survived the floor.
  - scenario: Revisit trigger (a) fires and the visual embedder is swapped; the doc requires every number be re-measured rather than carried over, but the next session cannot tell whether the ground-truth set is 25 or 26 items or which query set each figure spans, so the new floor is reported against a silently different denominator and the before/after comparison for the one value the repo demands ground-truth validation on becomes meaningless.

---

## Full review

I reviewed the single commit in `origin/main..HEAD` (`2787562`, R11.b per-footage-domain relevance floor): 11 files — `src/va/pipeline/retrieval.py`, `catalog_sqlite.py`, four `security.yaml` copies, `config/roles.yaml`, a new 344-line test file, and three docs.

## Verified before reporting (suspicions that dissolved)

- **Gate resolution is behavior-identical for A-EV.** `load_config()` and `config_for(None, "local")` both resolve to `footage_profile="generic"` because no config dir sets `active_footage_profile`, and `generic.yaml` is `roles: {}` — so the base path is unchanged, matching the commit's "byte-identical retrieval" claim.
- **Config-load legality in all four dirs.** All three `run-*/config/roles.yaml` declare `retriever:` (lines 57/57/58), so the new footage overlay clears `load_config`'s unknown-role check (`configuration.py:218-223`); `config/roles.yaml` leaves it commented and `config/profiles/footage/security.yaml` correctly omits the override, with cross-referencing notes in both files. `_load_footage_overlay`'s `enabled`/`GATEABLE_ROLES` validation doesn't touch a `retriever` overlay.
- **`videos.profile` is record==reality.** `ingest.py:237-249` records the *resolved* profile (explicit arg > `active_footage_profile` > source default), so `gates_by_video`'s `profile or default_footage_profile(stype)` cannot resolve a video against a config its roles never ran under.
- **Key types line up.** `_to_row` stores `str(v.id)`, `get_many` keys on that string, `EvidenceItem.video_id` is a `UUID`, so `str(it.video_id)` matches and `gates[None]` is a true fallback, not a catch-all.
- **Failure paths.** Catalog failure → base gate for all (`retrieval.py:222-225`); per-domain config failure → base gate with a warn trace (`147-157`); the inner `finally: catalog.close()` runs on both query failures. `get_relevance_gate`'s docstring promise holds.
- **`gates` vs `domains` status asymmetry is deliberate, not a bug.** `get_many` ignores `ingest_status` (gate what's actually in the pool) while `footage_domains()` restricts to `done` (suppress phantom warnings from a row created before fetch) — the two intents are opposite and each filter matches its own.
- **Blast radius.** `retrieve()` has one production caller (`ask.py:222`); nothing outside `retrieval.py` reads `attributes["fusion"]["gate"]`; no test asserts the old dict shape (`test_retrieval_e2e.py:148` checks only `rerank_weight`); `deep_scan.py:403` falls back through `primary_video_id`, computed pre-gate. The single-gate note string is byte-identical to the old format. Both shape changes are logged in COORDINATION.md, along with `Catalog.footage_domains()`.
- **Test integrity.** Purely additive — no test deleted or weakened. `test_security_footage_is_not_judged_by_the_a_ev_floor` sets a base floor of 1.01 the stub can never clear, so it fails against the old single-gate code rather than merely failing to import; the corrupt-DB test correctly writes garbage bytes because a *missing* workdir wouldn't reach the except branch.
- **Prior rounds' findings are closed.** The `GateMap` class docstring now says WORKDIR (round 7's finding); the untracked-file citations are gone from all committed artifacts, replaced by "kept OUTSIDE the repo" plus the tracked `tests/golden_queries/nvr0801_clip*.yaml` derivatives, which `git ls-files` confirms exist; the domain-blind gather/fusion major was adjudicated via its documented-limitation safe path (CLAUDE.md rule + workdir-keyed warning + plan backlog), so I don't re-raise it.
- **Combination coverage.** COORDINATION names both real-model golden runs (`.va-shots` 85/25/1, `.va-nvr -k nvr` 17/8xf) and states plainly what they do *not* prove — no golden test reaches `retrieve()` on security footage — naming `test_shipped_real_model_configs_carry_the_security_floor` (all three `run-*/config` dirs) as the stopgap.
- **Not verified by execution:** test runs required approval unavailable in this session, so pass status rests on reading.

## Finding

**minor — `video-analytics-model-analysis.md:94`** — the canonical decision block for the FLAGGED `min_cosine: 0.0` reports two different denominators for "ground-truth clips" without saying they come from different experiments: line 94 says the A-EV floor "retained **6/26** ground-truth clips" (the 9-query retrieval measurement) while line 107 says final evidence went "**7/25 → 13/25** at k=8" (the 8-question end-to-end run). Read in sequence, 7-in-final-evidence appears to exceed 6-surviving-the-floor, which is impossible for a subset — the same pair recurs in the commit body, all three `run-*/config/profiles/footage/security.yaml` comments, COORDINATION.md, and the test docstring.

*Failure scenario:* revisit trigger (a) fires — the visual embedder is swapped, and the doc says every number "must be re-measured, not carried over". The next session opens this block to reproduce the baseline, cannot tell whether the ground-truth set is 25 or 26 items or which query set each figure spans, and either re-derives it from the untracked household notes or reports a new floor against a silently different denominator, making the before/after comparison meaningless for the one value the repo requires ground-truth validation on.

*Safe path:* one clause on each figure — "6/26 clip-query pairs across the 9 retrieval queries" and "7/25 across the 8 end-to-end questions" — in the model-analysis block, which the profile comments already point to as the provenance.

```json
{"verdict": "approve", "findings": [{"severity": "minor", "file": "video-analytics-model-analysis.md", "line": 94, "issue": "The decision block recording the FLAGGED min_cosine: 0.0 uses two unexplained denominators for \"ground-truth clips\" (6/26 from the 9-query retrieval measurement, 7/25 from the 8-question end-to-end run), so the figures read as mutually contradictory — 7 in the final evidence appears to exceed the 6 that survived the floor.", "scenario": "Revisit trigger (a) fires and the visual embedder is swapped; the doc requires every number be re-measured rather than carried over, but the next session cannot tell whether the ground-truth set is 25 or 26 items or which query set each figure spans, so the new floor is reported against a silently different denominator and the before/after comparison for the one value the repo demands ground-truth validation on becomes meaningless."}]}
```
