# Agent review — approve

date: 2026-07-29T12:02:08.594657
range: origin/main..HEAD
branch: trust/l5-ergonomics
findings: 2

- **minor** `.claude/hooks/bash_guard.py:318` — The symlink-creation refusal scans only non-flag tokens of `ln`, so `ln -s --target-directory=reviews/disputes <ledger>` (attached-flag spelling) and `cp -s <ledger> reviews/disputes/x.md` (symlink via cp) both create a symlink inside the carve-out unblocked, contradicting matrix row 108's 'blocked at BOTH ends'; harmless in effect because any write through the link is still blocked by the resolved-path check in both guards (verified empirically).
  - scenario: Agent runs `ln -s --target-directory=reviews/disputes ../20260727-x.md` — allowed (verified, rc 0) — planting a ledger symlink inside disputes/; the attack then stalls because `echo forged > reviews/disputes/20260727-x.md` is blocked by realpath resolution, but the guard rule violates WT.12 item 3 (attached-value flags) the same commit codifies.
- **minor** `workflow-trust-plan.md:2296` — WT.11's Deliverable says the guard-rule checklist is 'referenced from CLAUDE.md', but CLAUDE.md is untouched in this range and contains no mention of WT.12 or the checklist.
  - scenario: A future session adds a guard rule following CLAUDE.md, never discovers the WT.12 pre-flight checklist, and reintroduces one of the four recurring defect shapes — the un-referenced advisory layer failure CLAUDE.md's own pruning rule warns about.

---

## Full review

Review complete. Here is what I verified and found.

## Verification of the round-1 findings (both genuinely fixed)

The prior ledger (`reviews/20260729-113949-…-27df820.md`) reported a major (symlink write-through onto a verdict ledger) and a minor (path_guard/bash_guard divergence on sentinels inside `disputes/`). No dispute files exist to re-judge. I re-ran both attacks empirically against the current hooks in a fixture repo rather than trusting the diff:

- `ln -s ../<ledger>.md reviews/disputes/link.md` → blocked (rc 2).
- Writing through a pre-planted symlink (`echo forged > reviews/disputes/link.md`, and the identical `Write` via path_guard) → blocked in **both** guards; the realpath resolution works.
- `reviews/disputes/.commit-approved` → blocked by path_guard (the sentinel check now runs before the carve-out).
- `reviews/disputes/../<ledger>.md` → blocked in both guards; legitimate dispute writes and abs-path spellings → allowed.
- The full trust-guard suite passes (246 tests), and the plan doc's embedded copies of both hooks match the live code line-for-line on all new lines.

## Findings that survived verification

**1. minor — `.claude/hooks/bash_guard.py:318` — the symlink-*creation* refusal is evadable by flag-attached and non-`ln` spellings, so matrix row 108's "blocked at BOTH ends" overclaims.** Empirically verified: `ln -s --target-directory=reviews/disputes ../20260727-x.md` is allowed (the token starts with `-` so the scan skips it — the exact attached-value-flag shape WT.12 item 3 warns about, codified in this same commit), and `cp -s ../20260727-x.md reviews/disputes/link2.md` is allowed (creates a symlink but isn't `ln`). Containment holds: every write **through** the resulting link is still blocked by the resolved-path check in both guards (verified), so no ledger can actually be rewritten — which is why this is minor, not major.

**2. minor — `workflow-trust-plan.md:2296` (WT.11 Deliverable) — the checklist is not "referenced from CLAUDE.md" as the deliverable specifies.** CLAUDE.md is untouched in this range and greps clean for "WT.12"/"checklist". Done-when (d) as literally worded is met (the checklist exists with all four recurring defects and their rounds), but the stated deliverable is not — and an unreferenced checklist is exactly the "advisory layer silently disabled" failure mode CLAUDE.md's own pruning rule describes: the next guard author won't find it.

Everything else checked out: the `norm()` normpath change introduces no regressions I could construct (`..`-to-sentinel, `~` forms, rule-3 deletes all behave); `reviews/disputes` itself (the bare directory) stays protected, so it cannot be deleted or replaced by a link; the reviewer-prompt and docstring edits accurately describe current behavior; no COORDINATION.md contracts, schemas, or tests are touched or weakened (the `test_lessons_section.py` change is docstring-only — it *narrows* a prior overclaim); Done-when (b), (c), (e) are all met with tests on both guards.

```json
{"verdict": "approve", "findings": [{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 318, "issue": "The symlink-creation refusal scans only non-flag tokens of `ln`, so `ln -s --target-directory=reviews/disputes <ledger>` (attached-flag spelling) and `cp -s <ledger> reviews/disputes/x.md` (symlink via cp) both create a symlink inside the carve-out unblocked, contradicting matrix row 108's 'blocked at BOTH ends'; harmless in effect because any write through the link is still blocked by the resolved-path check in both guards (verified empirically).", "scenario": "Agent runs `ln -s --target-directory=reviews/disputes ../20260727-x.md` — allowed (verified, rc 0) — planting a ledger symlink inside disputes/; the attack then stalls because `echo forged > reviews/disputes/20260727-x.md` is blocked by realpath resolution, but the guard rule violates WT.12 item 3 (attached-value flags) the same commit codifies."}, {"severity": "minor", "file": "workflow-trust-plan.md", "line": 2296, "issue": "WT.11's Deliverable says the guard-rule checklist is 'referenced from CLAUDE.md', but CLAUDE.md is untouched in this range and contains no mention of WT.12 or the checklist.", "scenario": "A future session adds a guard rule following CLAUDE.md, never discovers the WT.12 pre-flight checklist, and reintroduces one of the four recurring defect shapes — the un-referenced advisory layer failure CLAUDE.md's own pruning rule warns about."}]}
```
