# Agent review — approve

date: 2026-07-28T16:43:26.720976
range: origin/main..HEAD
branch: trust/l3-ci
findings: 3

- **minor** `.claude/hooks/bash_guard.py:340` — The credential-print guard matches --show-token by exact token equality, so the cobra-valid attached spelling `gh auth status --show-token=true` prints the token unblocked — the same attached-spelling class rounds 3-4 closed for -XPATCH/--method=.
  - scenario: An agent runs `gh auth status --show-token=true`; neither `--show-token` nor `-t` equals that token, the guard passes, and the credential the D9 speed bump exists to protect is printed to the transcript.
- **minor** `.claude/hooks/bash_guard.py:394` — Row 89's claim that label READS are allowed holds only for `gh pr/issue list`: the verb-less route match blocks read-only `gh api repos/o/r/issues/N/labels` GETs, contradicting the plan/as-built note.
  - scenario: An agent checking whether the human applied `human-reviewed` runs `gh api repos/deepak0414/video-analytics/issues/16/labels` (a GET); the guard blocks it as 'applying labels via the API', forcing the agent onto `gh pr view`/`list` even though the plan records reads as legitimate.
- **minor** `.claude/hooks/bash_guard.py:376` — mentions_label is decisive for any non-READ action, so `gh pr create --body "<filled PR template>"` is false-blocked because the repo's own template text contains the label names and `create` is not in READ_ACTIONS.
  - scenario: An agent finishes a branch and opens the PR with `gh pr create --title ... --body "$(cat body.md)"` where body.md is the filled .github/pull_request_template.md (whose checklist mentions human-reviewed/golden-verified); the guard blocks a legitimate, lifecycle-required action with a misleading 'labels are human-only' message; only the --body-file spelling works.

---

## Full review

Review complete. I examined the full range (one commit: WT.5–WT.7 CI gates), hand-traced `check_critical_paths.sh` and the new `gh`/HTTP guard rules against constructible spellings, verified the six committed ledgers' fix claims against HEAD, and checked the plan's as-built notes and "Done when" items. Sandbox permissions blocked running pytest, so test results rest on static reading plus the committed ledgers' recorded runs (223 trust tests green on this content).

Key verification results:

- **The two minors in the latest (untracked) ledger `reviews/20260728-163021-…` are already fixed in HEAD.** That ledger claims the quotepath fail-open and the `https`/`gh alias` gaps are "still valid at HEAD," but it was reviewing a stale tree (it references `awk` parsing that HEAD doesn't contain). The committed `scripts/check_critical_paths.sh:24` uses `git -c core.quotepath=off` with TAB-delimited `cut`/`tr` parsing, and `bash_guard.py:328,371` includes `https` in the client tuple and refuses `gh alias set` — with regression tests for all of it (`test_non_ascii_and_spaced_paths_are_matched`, the `gh alias` / `https` blocked-command rows). Re-judged on the merits: resolved.
- **The self-neutering circularity (CI runs the PR's own copy of the checker) is genuinely inherent**, not just excused: for `pull_request` events GitHub runs the PR's own workflow YAML, so pinning the script to the base branch gains nothing — a hostile PR could inline `exit 0` in the workflow itself. The local guards plus D9's honest "speed bump" scoping are the right posture, and the docs no longer overclaim.
- **Round-5 contamination is resolved in the range**: `tests/test_migrations.py` and the `schema.py`/`registry.py` edits are working-tree-only (the other session's), not in `origin/main..HEAD`; a clean checkout of HEAD collects without them.
- The checker's fail-open classes found across the backstop rounds (SIGPIPE under `pipefail`, unanchored substring match, rename destination-only reporting, C-quoted non-ASCII paths, heredoc-vs-stdin nesting) are each closed and each carries a test that reproduces the original failure mode — including the 64 KB-exceeding SIGPIPE reproduction.
- `pyproject.toml:74` registers the `golden` marker the workflow filters on; `tests/test_web.py:10` confirms the `[web]` install note; the `VA_AGENT_REVIEW` env-pop in `test_trust_hooks.py` only strengthens environment-independence.

Three minor findings survive, all in the acknowledged defense-in-depth layer (D9 scopes it as a speed bump, so none is a broken guarantee):

1. **`bash_guard.py:340`** — the credential-print guard matches `--show-token` by exact token equality, but cobra accepts the `--show-token=true` spelling, which prints the token and sails past — the same attached-spelling class rounds 3–4 closed for `-XPATCH`/`--method=`. One-line fix (`t.startswith("--show-token")`).
2. **`bash_guard.py:394`** — row 89 / the plan claim "label READS are no longer blocked, only applications" is only true for `gh pr/issue list`; a read-only `gh api repos/o/r/issues/14/labels` GET (no `-X`, no body) is still blocked by the verb-less route match, so the sanctioned way to check whether the human applied a label doesn't include the API route the docs imply.
3. **`bash_guard.py:376`** — `mentions_label` is decisive for any non-READ action, so `gh pr create --body "<filled template>"` is false-blocked: the repo's own PR template text contains "human-reviewed"/"golden-verified", and `create` is not a READ_ACTION. Opening PRs is a legitimate agent action in this lifecycle; the `--body-file` spelling works, but the natural spelling fails with a misleading "labels are human-only" message.

No correctness bugs in the checker or workflows, no contract breaks (COORDINATION.md carries the cross-agent announcement), no test weakening, no silently hardcoded content (`critical_paths.txt` is deliberate, documented, and itself gated). The WT.5–7 "Done when" items that remain (red-check/merge-button behavior, label-toggles-green) can only be demonstrated on the live PR, and this PR — touching `scripts/` and `.claude/` — will exercise them itself.

```json
{"verdict": "approve", "findings": [
{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 340, "issue": "The credential-print guard matches --show-token by exact token equality, so the cobra-valid attached spelling `gh auth status --show-token=true` prints the token unblocked — the same attached-spelling class rounds 3-4 closed for -XPATCH/--method=.", "scenario": "An agent runs `gh auth status --show-token=true`; neither `--show-token` nor `-t` equals that token, the guard passes, and the credential the D9 speed bump exists to protect is printed to the transcript."},
{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 394, "issue": "Row 89's claim that label READS are allowed holds only for `gh pr/issue list`: the verb-less route match blocks read-only `gh api repos/o/r/issues/N/labels` GETs, contradicting the plan/as-built note.", "scenario": "An agent checking whether the human applied `human-reviewed` runs `gh api repos/deepak0414/video-analytics/issues/16/labels` (a GET); the guard blocks it as 'applying labels via the API', forcing the agent onto `gh pr view`/`list` even though the plan records reads as legitimate."},
{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 376, "issue": "mentions_label is decisive for any non-READ action, so `gh pr create --body \"<filled PR template>\"` is false-blocked because the repo's own template text contains the label names and `create` is not in READ_ACTIONS.", "scenario": "An agent finishes a branch and opens the PR with `gh pr create --title ... --body \"$(cat body.md)\"` where body.md is the filled .github/pull_request_template.md (whose checklist mentions human-reviewed/golden-verified); the guard blocks a legitimate, lifecycle-required action with a misleading 'labels are human-only' message; only the --body-file spelling works."}
]}
```
