# Agent review — approve

date: 2026-07-28T16:50:04.460132
range: origin/main..HEAD
branch: trust/l3-ci
findings: 3

- **minor** `.claude/hooks/bash_guard.py:370` — Only `gh alias set` is blocked; `gh alias import aliases.yml` installs the same guard-hiding aliases from a file, reopening the indirection the finalize-round fix closed.
  - scenario: An agent writes aliases.yml containing `m: pr merge`, runs `gh alias import aliases.yml` (no rule fires), then `gh m 14` — noun 'm' matches no rule and the human-only merge goes through.
- **minor** `.claude/hooks/bash_guard.py:387` — `gh pr edit N --body "<filled template>"` is false-blocked because the template text names both review labels, mentions_label is decisive, and `edit` is not a READ action — yet a body cannot apply a label, and body-fixes-without-new-commits are exactly what the `edited` CI re-run supports.
  - scenario: The evidence check fails on a PR; the agent regenerates the block with /verify and runs `gh pr edit 16 --body "$(cat body.md)"` where body.md quotes the checklist mentioning human-reviewed — the guard blocks a legitimate lifecycle action with a 'labels are human-only' message; only the --body-file spelling works.
- **minor** `.claude/hooks/bash_guard.py:328` — The HTTP-client rule matches label strings and /labels|/reviews|/merge routes but not GraphQL mutation names, so the curl-to-graphql spelling of addLabelsToLabelable (label by node ID) passes — while the identical `gh api graphql` spelling is blocked.
  - scenario: An agent posts `curl https://api.github.com/graphql -d '{"query":"mutation{addLabelsToLabelable(input:{labelableId:...,labelIds:[...]})}"}'`: no review-label string and no /labels route appear, the guard stays silent, and the label is applied — a speed-bump gap per D9, but one the gh-side rule already closes for its own spelling.

---

## Full review

Review complete. I examined the single-commit range (WT.5–WT.7 CI gates), hand-traced `check_critical_paths.sh` and the new `gh`/HTTP guard rules against constructible command spellings, cross-checked the plan's task cards and both pending ledgers, and verified the contract/test-integrity axes. Sandbox permissions blocked running pytest, so test results rest on static reading plus the prior ledgers' recorded runs.

## Review: `origin/main..b08a5e7` (WT.5–WT.7 CI gates)

**Verdict: approve** — three minor findings, all in the defense-in-depth layer that D9 explicitly scopes as a speed bump. No correctness bugs in the checker or workflows, no contract breaks, no test weakening.

**Prior-ledger findings re-judged on the merits — all five are genuinely fixed at HEAD:**
- Ledger `20260728-163021`'s quotepath/whitespace fail-open: fixed (`git -c core.quotepath=off` + TAB-delimited `cut`/`tr` parsing, `scripts/check_critical_paths.sh:24-31`) with a reproducing test (`test_non_ascii_and_spaced_paths_are_matched`). Its `https`/`gh alias set` gaps: fixed (`bash_guard.py:328`, `:370`) with blocked-command rows.
- Ledger `20260728-163754`'s three minors: `--show-token=true` now caught by `startswith("--show-token=")` (`bash_guard.py:339`), read-only `gh api …/labels` GETs now allowed via the explicit-method default (`bash_guard.py:402-405`, allowed-row test), and `create` was added to `READ_ACTIONS` to unblock `gh pr create --body` (`bash_guard.py:386`). None of those findings should be repeated.

**What I verified independently:**
- **The checker's logic is sound.** Comment/blank lines, anchored prefix (not substring) matching, rename both-path handling, fail-closed on missing table or undiffable base, and the documented SIGPIPE/no-`set -e` reasoning all hold; each previously-found fail-open class carries a regression test, including the 64 KB-exceeding SIGPIPE reproduction.
- **The workflows are injection-safe and install-correct.** The PR body reaches `grep` via `env:` (not inline interpolation); `pyproject.toml` provides the `dev`/`web` extras and registers the `golden` marker the `-m "not golden"` filter needs; `critical-paths` checks out `head.sha` with `fetch-depth: 0`, which the three-dot diff requires.
- **No contract or schema issues in the range**: the working-tree edits to `schema.py`/`registry.py`/`test_migrations.py` belong to another session and are NOT in `origin/main..HEAD`. COORDINATION.md carries the cross-agent announcement.
- **`critical_paths.txt` is hardcoded content done right**: explicitly flagged, documented, and itself added to both guards so a PR can't silently neuter the gate it must pass.
- **Live-only "Done when" items** (red check disables merge, label toggles green without a commit, branch protection) can only be demonstrated on the first real PR; the plan records this honestly, and this PR — touching `scripts/` and `.claude/` — will exercise the gate on itself.

**Three surviving minors** (all one-to-few-line fixes for the finalizing amend or the next trust PR):

1. **`bash_guard.py:370` — `gh alias import` reopens the hole `gh alias set` closes.** Only `words[:2] == ["alias", "set"]` is blocked; `gh alias import aliases.yml` (real gh subcommand) installs the same `m: pr merge` alias from a file, then `gh m 14` bypasses every noun/action rule — the exact indirection the finalize-round fix was for.
2. **`bash_guard.py:387` — `gh pr edit N --body "<filled template>"` is false-blocked.** `mentions_label` is decisive for any non-READ action and `edit` is (correctly, for `--add-label`) not a READ action — but a `--body` whose text quotes the template's checklist (which names both labels) can't apply a label, and "fix the PR body without a new commit" is exactly the workflow the `edited` re-run exists for. The `create` fix didn't cover the body-fix path; only the `--body-file` spelling works, with a misleading block message.
3. **`bash_guard.py:328-334` — the HTTP-client rule misses the GraphQL-by-node-ID route.** `curl https://api.github.com/graphql -d '{"query":"mutation{addLabelsToLabelable(...)}"}'` names the label only by node ID, so neither the label-string check nor the `/(labels|reviews|merge)\b` route regex fires — while the `gh api graphql` spelling of the same mutation IS blocked. D9 scopes this whole layer as a speed bump, hence minor; adding the four mutation names to the HTTP check mirrors the gh rule.

```json
{"verdict": "approve", "findings": [
{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 370, "issue": "Only `gh alias set` is blocked; `gh alias import aliases.yml` installs the same guard-hiding aliases from a file, reopening the indirection the finalize-round fix closed.", "scenario": "An agent writes aliases.yml containing `m: pr merge`, runs `gh alias import aliases.yml` (no rule fires), then `gh m 14` — noun 'm' matches no rule and the human-only merge goes through."},
{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 387, "issue": "`gh pr edit N --body \"<filled template>\"` is false-blocked because the template text names both review labels, mentions_label is decisive, and `edit` is not a READ action — yet a body cannot apply a label, and body-fixes-without-new-commits are exactly what the `edited` CI re-run supports.", "scenario": "The evidence check fails on a PR; the agent regenerates the block with /verify and runs `gh pr edit 16 --body \"$(cat body.md)\"` where body.md quotes the checklist mentioning human-reviewed — the guard blocks a legitimate lifecycle action with a 'labels are human-only' message; only the --body-file spelling works."},
{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 328, "issue": "The HTTP-client rule matches label strings and /labels|/reviews|/merge routes but not GraphQL mutation names, so the curl-to-graphql spelling of addLabelsToLabelable (label by node ID) passes — while the identical `gh api graphql` spelling is blocked.", "scenario": "An agent posts `curl https://api.github.com/graphql -d '{\"query\":\"mutation{addLabelsToLabelable(input:{labelableId:...,labelIds:[...]})}\"}'`: no review-label string and no /labels route appear, the guard stays silent, and the label is applied — a speed-bump gap per D9, but one the gh-side rule already closes for its own spelling."}
]}
```
