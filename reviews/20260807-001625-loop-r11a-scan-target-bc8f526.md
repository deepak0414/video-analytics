# Agent review — approve

date: 2026-08-07T00:35:18.905975
range: origin/main..HEAD
branch: loop/r11a-scan-target
findings: 2

- **minor** `src/va/adapters/reasoner/rule_inproc.py:81` — The stage-2 degrade can still emit a counting-noise-only scan target when the query's only referent is a pronoun (stage-2 survivors are by construction limited to {color,colour,number} plus _SCAN_NOISE's counting extras), contradicting the change's own 'no derivable subject = no sweep' rule.
  - scenario: 'how many times did it change?' matches _DEEP_SCAN, stage 1 empties, stage 2 derives 'the times change'; on a real-captioner path the VLM sweeps ~120 frames on a subject-free micro-prompt and the arbitrary counts are rendered as the leading CODE-COUNTED line. Fix: after stage 2, return None unless at least one survivor is a subject word (not in _SCAN_NOISE, or in {color, colour, number}).
- **minor** `architecture-evolution-loop.md:189` — Carried from round 1 (undisputed): R11.a's second Done-when — 'A-EV golden-ask harness still green' — still has no recorded run on the branch; the harness is gated (RUN_GOLDEN=1, real workdir) and nothing in this commit evidences it.
  - scenario: Static reading says the golden-ask targets and cache keys are unchanged, but if a derivation or cache-key subtlety was missed the gated harness silently regresses and nothing in this change would show it. Fix: run the gated golden-ask harness once the live pytest sessions drain and record the counts in the /task-commit digest.

---

## Full review

Review complete. This is round 2 for R11.a; I verified the round-1 findings' dispositions against the rework, read the full deep-scan/ask/rule-planner sources, the configuration layer (`FootageSettings`, `config_for`, `default_footage_profile`), all four config dirs' footage yamls, the plan item, and the dispute ledger (no disputes recorded for this branch). Five pytest processes are live in other sessions, so per the 2026-08-04 lesson I did not launch a run; verification is static.

**What the rework got right:**
- Round-1 major (stage-2 emitted pure counting-noise, subject nouns dropped): fixed via `_TARGET_GRAMMAR` — "color"/"colour"/"number" now survive stage 2; the canonical query derives "the times color change video" (subject present). The deliberate design comment explains the set choice.
- Round-1 minor (three skip causes behind one `None`): fixed properly — `run_deep_scan` now returns `(result, skip_reason)`, the note names the specific cause, and `test_security_profile_gates_deep_scan_off` asserts the profile-specific cause. All call sites updated (ask.py, tests); no stale assertions on the old note string anywhere.
- Profile gate resolves the video's **recorded** profile via `config_for` (record==reality, consistent with stale/reprocess); `SourceType` is a str-enum so `.value` is safe; all four config dirs carry `deep_scan: "off"` in security.yaml; a `None` profile falls back to the source-derived default, so NVR-ingested pre-profile rows still gate correctly. `DEFAULT_TARGET` is gone from the tree entirely. COORDINATION.md and CLAUDE.md updated in the same change.

**Two findings survived, both minor:**

1. **The stage-2 degrade can still emit a subject-free, counting-noise-only target** (`rule_inproc.py:81`). By set construction, stage-2 survivors ⊆ {color, colour, number} ∪ (`_SCAN_NOISE`'s counting extras: times/change/video/scene/…). When the query's only referent is a pronoun — "how many times did it change?" — stage 1 empties and stage 2 yields "the times change": no subject at all, contradicting the change's own documented rule ("no derivable query subject = no sweep"). On a real-captioner path a VLM sweeps ~120 frames on a nonsense micro-prompt and the arbitrary counts lead the answer as CODE-COUNTED. Safe path: after stage 2, return `None` unless at least one survivor is a subject word (outside `_SCAN_NOISE`, or in {color, colour, number}); the input class is narrow (subject-free queries), hence minor rather than major.

2. **Plan conformance, carried from round 1 (undisputed, unaddressed):** R11.a's second Done-when — "A-EV golden-ask harness still green" — still has no recorded run on the branch. Static reading says targets and cache keys for the two golden ask questions are unchanged (stage 1 derives them identically to the old code), but the Done-when asks for the run. Safe path: run the gated harness (`RUN_GOLDEN=1 … -m golden`) once the live pytest sessions drain and carry the counts in the /task-commit digest.

The first Done-when (security profile → outfit target can no longer fire) is met and pinned by a cause-specific end-to-end test.

```json
{"verdict": "approve", "findings": [
  {"severity": "minor", "file": "src/va/adapters/reasoner/rule_inproc.py", "line": 81, "issue": "The stage-2 degrade can still emit a counting-noise-only scan target when the query's only referent is a pronoun (stage-2 survivors are by construction limited to {color,colour,number} plus _SCAN_NOISE's counting extras), contradicting the change's own 'no derivable subject = no sweep' rule.", "scenario": "'how many times did it change?' matches _DEEP_SCAN, stage 1 empties, stage 2 derives 'the times change'; on a real-captioner path the VLM sweeps ~120 frames on a subject-free micro-prompt and the arbitrary counts are rendered as the leading CODE-COUNTED line. Fix: after stage 2, return None unless at least one survivor is a subject word (not in _SCAN_NOISE, or in {color, colour, number})."},
  {"severity": "minor", "file": "architecture-evolution-loop.md", "line": 189, "issue": "Carried from round 1 (undisputed): R11.a's second Done-when — 'A-EV golden-ask harness still green' — still has no recorded run on the branch; the harness is gated (RUN_GOLDEN=1, real workdir) and nothing in this commit evidences it.", "scenario": "Static reading says the golden-ask targets and cache keys are unchanged, but if a derivation or cache-key subtlety was missed the gated harness silently regresses and nothing in this change would show it. Fix: run the gated golden-ask harness once the live pytest sessions drain and record the counts in the /task-commit digest."}
]}
```
