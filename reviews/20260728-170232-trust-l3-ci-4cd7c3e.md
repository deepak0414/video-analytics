# Agent review — approve

date: 2026-07-28T17:09:58.166354
range: origin/main..HEAD
branch: trust/l3-ci
findings: 2

- **minor** `.claude/hooks/bash_guard.py:379` — The stale comment 'Any mention of a review label anywhere in a gh command is decisive' describes the deleted blanket rule and contradicts the corrected prose-is-not-an-action rule below it, while the plan's closing-round note claims this comment was removed (its mirror also persists at workflow-trust-plan.md:780 and in matrix row 85's rationale).
  - scenario: A future guard editor trusts the comment, reinstates blanket label-text matching, and reintroduces the false-block on `gh pr create/edit --body "<filled template>"` that rows 100/102 exist to prevent.
- **minor** `tests/test_trust_guards.py:657` — The space-path assertion in test_non_ascii_and_spaced_paths_are_matched cannot fail independently: the earlier schéma.py commit remains in base...HEAD, so check('') returns 1 via that file regardless of whether space-containing paths are parsed correctly.
  - scenario: The checker regresses to whitespace-split path parsing; this test stays green because schéma.py alone forces exit 1, while an exact-file critical pattern containing a space fails open — the same non-reproducing-regression-test class the plan documents for the first SIGPIPE test. Fix: call mark_base() before the space-path change.

---

## Full review

Review complete. Verdict: **approve** — two minor findings, no majors.

## What I reviewed and verified

The range is one commit (4cd7c3e): the WT.5–WT.7 CI gates — two GitHub workflows, the critical-path checker + table, `/verify`, the PR template, extensions to the bash/path guards, and 11 new gate tests. This is the fifth review round on this content; I re-judged the prior ledgers' findings on the merits rather than repeating them.

**Prior findings are genuinely fixed at HEAD** (verified against the committed tree, not the ledgers' claims): `--approve=true` is now caught by `startswith(("--add-label=", "--approve="))` with a blocked-row regression test (`test_trust_guards.py:110`); the "un-bypassable CI" overclaim is replaced by the honest-limit framing in `offline-tests.yml`, `CLAUDE.md`, and the plan; the dead `mentions_label` variable is removed. Earlier rounds' fixes (quotepath, SIGPIPE, rename handling, `gh alias`, `https` client, GraphQL-on-curl, `gh api` GET reads) all verified present with regression rows wired into real parametrized subprocess tests.

**What held up under my own probing** (pytest execution is permission-blocked in this session, so this is static tracing plus the test wiring): the checker's prefix matching, fail-closed paths, and rename/quotepath handling are sound; I hunted for `gh_words` misparses that could hide `merge`/`edit` and found only spellings gh itself would reject as invalid flags; the `gh api` verb-inference (explicit `-X`/`--method`, else POST-when-fields, else GET) correctly separates label reads from applications; the workflows are injection-safe (PR body and labels reach the shell via `env:`), the `golden` marker and `web`/`dev` extras the workflow depends on exist in `pyproject.toml`, and the job ids match the branch-protection contexts. The other session's `schema.py`/`registry.py`/`test_migrations.py` edits are working-tree-only, not in the range. No tests deleted or weakened; the `VA_AGENT_REVIEW` env-pop is a legitimate environment-independence fix.

## Findings

1. **minor — `.claude/hooks/bash_guard.py:379`** — The comment "Any mention of a review label anywhere in a gh command is decisive" survived the closing-round cleanup: the plan's as-built note (and matrix row 85's rationale) claims this comment was removed along with `mentions_label`, but it still sits directly above the merge check, contradicting the corrected "Prose is not an action" rule twelve lines below and the plan-doc mirror at `workflow-trust-plan.md:780` repeats it. Scenario: a future guard editor trusts the older comment, reinstates blanket label-text matching, and reintroduces the `gh pr create/edit --body "<filled template>"` false-block that rows 100/102 exist to prevent.

2. **minor — `tests/test_trust_guards.py:657`** — The space-path half of `test_non_ascii_and_spaced_paths_are_matched` cannot fail: the `schéma.py` commit is still in `base...HEAD` when `my model.py` is added, so the final `returncode == 1` assertion passes regardless of whether space-containing paths are parsed correctly — the same "regression test that cannot reproduce its failure is decoration" class the plan records for the first SIGPIPE test. Scenario: a future edit regresses the checker to whitespace-split parsing; this test stays green (via schéma.py) while an exact-file critical pattern containing a space fails open. Fix: `mark_base()` before the space-path change (the fixture already provides it).

Both are one-line-fix minors in already-acknowledged defense-in-depth/doc territory; per the lifecycle rule (request_changes iff critical or major), this approves.

```json
{"verdict": "approve", "findings": [
{"severity": "minor", "file": ".claude/hooks/bash_guard.py", "line": 379, "issue": "The stale comment 'Any mention of a review label anywhere in a gh command is decisive' describes the deleted blanket rule and contradicts the corrected prose-is-not-an-action rule below it, while the plan's closing-round note claims this comment was removed (its mirror also persists at workflow-trust-plan.md:780 and in matrix row 85's rationale).", "scenario": "A future guard editor trusts the comment, reinstates blanket label-text matching, and reintroduces the false-block on `gh pr create/edit --body \"<filled template>\"` that rows 100/102 exist to prevent."},
{"severity": "minor", "file": "tests/test_trust_guards.py", "line": 657, "issue": "The space-path assertion in test_non_ascii_and_spaced_paths_are_matched cannot fail independently: the earlier schéma.py commit remains in base...HEAD, so check('') returns 1 via that file regardless of whether space-containing paths are parsed correctly.", "scenario": "The checker regresses to whitespace-split path parsing; this test stays green because schéma.py alone forces exit 1, while an exact-file critical pattern containing a space fails open — the same non-reproducing-regression-test class the plan documents for the first SIGPIPE test. Fix: call mark_base() before the space-path change."}
]}
```
