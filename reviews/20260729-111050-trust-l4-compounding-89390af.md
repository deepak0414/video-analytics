# Agent review — approve

date: 2026-07-29T11:14:49.751939
range: origin/main..HEAD
branch: trust/l4-compounding
findings: 1

- **minor** `tests/test_lessons_section.py:89` — The conformance test's docstring claims the section 'contains nothing else', but the check only validates bullet-shaped lines — a lesson added as a plain prose paragraph (or a tab-indented continuation, which entries() silently drops) escapes every check.
  - scenario: A hand-edited lesson written as an undashed paragraph at the section's bottom is invisible to the budget, length, date, dedup, and conformance checks, so the section can grow past its budget while the suite stays green; the docstring overstates the enforced guarantee.

---

## Full review

Review complete. This reviews the amended WT.8 commit (89390af) — the `/lesson` command, the CLAUDE.md Lessons section (7 seeded entries), `tests/test_lessons_section.py`, and the plan's as-built/descope record.

**What I verified:**

- **Prior round's minor is fixed.** The last ledger entry (c61ed6c) flagged that the conformance test only inspected `- ` bullets at column 0. The amended test now matches any bullet shape (`^\s*[-*+]\s`), so indented and `*`/`+` bullets are caught. Confirmed by inspection and by executing the test's exact logic against the committed CLAUDE.md.
- **All six checks pass against the committed content**: 7 entries, longest 337/400 chars, valid ISO dates, no duplicates, no malformed bullets. (The plan's narrative says "longest is 301/400" — a stale round-2 measurement, but it's chronological prose and the substantive claim "the budget can bite" holds.) The enforcement mechanism for a planted malformed/undated bullet works: it matches the bullet-shape regex, fails the dated-format regex, and trips the assert.
- **Descope is clean.** The commit range adds exactly two files and deletes nothing; no loop-guard hook code or orphaned tests remain. The WT.9 deferral row, CLAUDE.md's "advisory until then" note, and the scope-decision block are mutually consistent.
- **Citation restoration verified.** Rows 100–105 exist in the §8 matrix at `workflow-trust-plan.md:2433-2438`; the four restored citations (lines 2011–2026) all point at rows that exist; the only mention of the removed rows 106–124 is the intentional prose note. All 8 "deferred-hook note" retargets resolve to the WT.8 scope-decision block.
- **Plan conformance.** `.claude/commands/lesson.md` is byte-identical to the WT.8 spec in the plan; the "Done when" behavior (append + dedup-on-reuse) is evidenced in the as-built note.
- **Disputed round-4 findings**: all four concerned the loop-guard hook, which is removed from this range — moot; not re-raised.
- No tests deleted or weakened; no contract/schema/config changes in the range (the working-tree modifications to `registry.py`, `schema.py`, `test_trace_link.py` are uncommitted and outside it). No silent hardcoded content — the 20-entry and 400-char budgets are documented structural budgets.

**One residual minor**, the last slice of the gap the previous two rounds narrowed:

- **minor** — `tests/test_lessons_section.py:89`: the docstring claims "Assert the section contains nothing else," but the check only validates *bullet-shaped* lines — a lesson hand-edited in as a plain prose paragraph (or a tab-indented continuation, which `entries()` silently drops from the measured length) is invisible to every check. Scenario: a lesson written as an undashed paragraph at the section's bottom grows the section indefinitely with the suite green — and the docstring overstates coverage, which is the doc-asserts-what-code-doesn't shape this project tracks. The realistic accident paths (undated bullet, alternate bullet marker) are all caught, so this does not block.

```json
{"verdict": "approve", "findings": [{"severity": "minor", "file": "tests/test_lessons_section.py", "line": 89, "issue": "The conformance test's docstring claims the section 'contains nothing else', but the check only validates bullet-shaped lines — a lesson added as a plain prose paragraph (or a tab-indented continuation, which entries() silently drops) escapes every check.", "scenario": "A hand-edited lesson written as an undashed paragraph at the section's bottom is invisible to the budget, length, date, dedup, and conformance checks, so the section can grow past its budget while the suite stays green; the docstring overstates the enforced guarantee."}]}
```
