# Agent review — approve

date: 2026-08-10T15:07:11.690878
range: origin/main..HEAD
branch: trust/ci-flake-critical-paths
findings: 3

- **minor** `scripts/check_critical_paths.sh:87` — The operator-facing FAIL message still asserts 'agents are guard-blocked from setting it' — the exact claim this commit retracted per D9 in the plan's Done-when and in critical_paths.txt's header.
  - scenario: A human hits the red critical-paths job, reads the checker's own advice, and concludes the label is mechanically agent-proof — the belief D9 exists to correct, restated at the one moment it matters. Fix: qualify the line the same way the table header now is (guards are a speed bump against accident, not a guarantee), and re-copy into the plan mirror at line 1950 so the new parity test stays green.
- **minor** `tests/test_trust_guards.py:567` — The stderr-surfacing fix was applied in two individual tests, not in the shared check() helper, though workflow-trust-plan.md:1988 describes it as a fixture-level change.
  - scenario: The flake recurs on test_rename_of_a_critical_file_is_caught or test_critical_path_requires_its_label instead — those assert on res.stdout only, so git's fatal: sits unread in res.stderr and diagnosis #4 is guesswork again, exactly as the as-built note describes. Fix: build the stdout+stderr detail string inside check() (or have it return a wrapper whose repr carries both) so every cp_repo assertion inherits it, then narrow the plan's wording to match whichever is done.
- **minor** `.guard-override:0` — The gate-maintenance override sentinel is still present in the worktree after the gate-machinery edit that needed it (gitignored, so it does not appear in git status).
  - scenario: The next session in this worktree inherits relaxed path guards on .githooks/, .claude/, .github/workflows/ and the trust scripts and can edit them without the human noticing. Fix: remove .guard-override now that scripts/check_critical_paths.sh is committed.

---

## Full review

## Review — `origin/main..HEAD` (1 commit, `a59f9e7`)

**Scope:** `scripts/check_critical_paths.sh` (+29), `tests/test_trust_guards.py` (+90), `tests/test_trust_hooks.py` (+33), `workflow-trust-plan.md` (+88). No roles, adapters, config dirs, or footage profiles are touched, so the backend/profile matrix is genuinely unaffected — the only combination axis here is CI job (`critical-paths` vs `offline-tests`), and both call sites are covered by the new tests.

**Caveat on method:** this session's Bash permissions blocked test execution (`pytest`, `ps`, `cksum` all denied), so my assessment of the new tests is from reading, not from running them. I verified the two `workflow-trust-plan.md` mirror blocks line-for-line against `scripts/check_critical_paths.sh` (plan 1864–1953 vs. file 1–90) and `scripts/critical_paths.txt` (plan 1808–1849 vs. file 1–42), including the header-offset arithmetic (`header_lines` 3 and 2) and the single-occurrence assertion — both mirrors currently match, and each marker appears exactly once.

**What holds up.** The gate still fails closed on every path I could construct: a failed `mktemp` makes the redirect fail, which takes the `||` branch and exits 1; a missing table still exits 1 before any temp file exists. Redirecting git's stderr to a file rather than `2>&1` is the right call and the reasoning in the comment is correct — a warning line has no TAB, `cut -f2-` passes it through whole, and it would then be prefix-matched as a path. The `if`-form for the success-path replay avoids the `[ -s … ] && …` exit-status trap. The `-M`/`D`+`A` claim is right: a skipped rename detection is not a gate hole. `test_git_warnings_reach_the_log_without_entering_the_path_list` does construct its scenario (40 rename-and-edit pairs with `diff.renameLimit=1`, so no exact-rename shortcut) and would genuinely fail on a regression to `2>&1` or to no redirect at all — the docstring is also honest about the half it cannot observe. Strengthening the fixture's `git()` to assert success does not break any existing test: I checked every `change()`/`git_mv()` call site for empty commits and found none. Root cause is explicitly reported as still open rather than dressed up as fixed, which is the right call under the "determinism is not correctness" convention. The commit subject is a provisional `need_agent_review:` and is exempt from the plain-description rule.

**Findings — three minor, no critical or major.**

1. `scripts/check_critical_paths.sh:87` still tells the operator "agents are guard-blocked from setting it." That is the exact claim this commit went out of its way to retract in the other two places it appeared (WT.7's Done-when, and the `critical_paths.txt` header), citing D9. It survives in the one message a human actually reads in a red CI job, and is mirrored into the plan at line 1950.
2. `workflow-trust-plan.md:1988` says "the fixture surfaces `res.stderr` in its failure output," but the fix landed in two individual tests, not in `check()` (`tests/test_trust_guards.py:567`). The other nine `cp_repo` tests drive the same checker and still assert on `res.stdout` alone — if the flake lands on one of those, the evidence is lost again, which is precisely the failure this note diagnoses.
3. `.guard-override` is still present in the worktree (gitignored, so it does not show in `git status`). Editing gate machinery required it; CLAUDE.md requires removing it after. Out of the diff, but created by doing this work.

**Process note (not a finding):** the diff touches `scripts/`, so this PR needs the `human-reviewed` label — the human's act, in the GitHub UI. No `golden-verified` path is touched. No COORDINATION.md entry is needed: no shared interface changed. No new env vars, CLI flags, or config keys, so there is no documentation-parity gap.

```json
{"verdict": "approve", "findings": [{"severity": "minor", "file": "scripts/check_critical_paths.sh", "line": 87, "issue": "The operator-facing FAIL message still asserts 'agents are guard-blocked from setting it' — the exact claim this commit retracted per D9 in the plan's Done-when and in critical_paths.txt's header.", "scenario": "A human hits the red critical-paths job, reads the checker's own advice, and concludes the label is mechanically agent-proof — the belief D9 exists to correct, restated at the one moment it matters. Fix: qualify the line the same way the table header now is (guards are a speed bump against accident, not a guarantee), and re-copy into the plan mirror at line 1950 so the new parity test stays green."}, {"severity": "minor", "file": "tests/test_trust_guards.py", "line": 567, "issue": "The stderr-surfacing fix was applied in two individual tests, not in the shared check() helper, though workflow-trust-plan.md:1988 describes it as a fixture-level change.", "scenario": "The flake recurs on test_rename_of_a_critical_file_is_caught or test_critical_path_requires_its_label instead — those assert on res.stdout only, so git's fatal: sits unread in res.stderr and diagnosis #4 is guesswork again, exactly as the as-built note describes. Fix: build the stdout+stderr detail string inside check() (or have it return a wrapper whose repr carries both) so every cp_repo assertion inherits it, then narrow the plan's wording to match whichever is done."}, {"severity": "minor", "file": ".guard-override", "line": 0, "issue": "The gate-maintenance override sentinel is still present in the worktree after the gate-machinery edit that needed it (gitignored, so it does not appear in git status).", "scenario": "The next session in this worktree inherits relaxed path guards on .githooks/, .claude/, .github/workflows/ and the trust scripts and can edit them without the human noticing. Fix: remove .guard-override now that scripts/check_critical_paths.sh is committed."}]}
```
