# Agent review — approve

date: 2026-07-29T12:21:05.194664
range: origin/main..HEAD
branch: trust/l5-ergonomics
findings: 1

- **minor** `tests/test_trust_hooks.py:590` — The round-4 MAJOR fix (reviews/disputes/ added to the never-inert list in all three hooks) and the row-111 linker spellings (ln -s --target-directory=, cp -s) shipped without regression tests — only the :(glob) hash fix got one — despite WT.12 item 5, codified in this same commit, requiring plant-the-defect tests.
  - scenario: A later change 'simplifies' one hook's instruction-bearing regex back to '^(\.claude/|\.githooks/|\.github/|scripts/)|^CLAUDE\.md$' (e.g. copy-pasted from an older plan revision); the full suite stays green because no test commits a disputes-only branch and asserts the docs-only exemption is denied, and disputes-only branches silently regain the skip-review path to main — the exact end state round 4 rated major.

---

## Full review

Verification is complete. Everything the amend claims checks out empirically; one minor test-coverage gap remains.

## Review summary — HEAD 38b4515 (round 5)

I reconstructed the round history: round 4 (a276913) requested changes with one major (docs-only exemption let a disputes-only branch skip review) and two minors (no regression test for the `:(glob)` hash fix; linker rule missed attached-flag/`cp -s` spellings). `reviews/disputes/` contains only the README — no rebuttals to re-judge. I verified each prior finding against HEAD rather than restating:

**All three round-4 findings are genuinely fixed:**

- **Major — fixed.** `reviews/disputes/` joins the never-inert list in all three hooks (`commit-msg`, `post-commit`, `pre-push`; the plan doc's embedded copies match). Verified the regex: `reviews/disputes/evil.md` matches (review fires), a verdict ledger and `notes.md` do not.
- **Minor 1 — fixed.** `test_dispute_files_enter_the_approval_hash` asserts both directions (verdict ledgers stay out of the hash, disputes enter it). Verified independently: `git ls-files ':(glob)reviews/*.md'` matches all 50 top-level ledgers and zero disputes files.
- **Minor 2 — fixed.** The linker rule now reads all tokens. I probed the guard directly: `ln -s --target-directory=reviews/disputes …`, `ln -st`, `cp -s`, `cp --symbolic-link`, `ln --symbolic`, env-prefixed, multi-line, and quoted spellings all block (rc 2); hardlink and `mv reviews/disputes` block too.

**Carve-out behavior verified empirically on both guards:** dispute writes allowed (relative, absolute, `tee`, `mv`-into, nested); traversal (`disputes/../`), sentinels inside disputes, the disputes dir itself, and ledger writes all blocked; the symlink write-through case is covered by `test_symlink_into_disputes_cannot_reach_a_verdict_ledger`. Full offline suite: **446 passed, 2 skipped**.

**One finding survives (minor):** the round-4 **major** fix shipped without a regression test. Nothing in `tests/test_trust_hooks.py` asserts a disputes-only branch is denied the docs-only exemption (plain-subject commit blocked / review fires at push), and `BLOCKED_COMMANDS` gained only the plain `ln -s` spelling — not the row-111 spellings (`--target-directory=`, `cp -s`) fixed in this same amend. The glob fix got its plant-the-defect test; the other two fixes did not, in the commit that codifies WT.12 item 5. Dropping `reviews/disputes/` from one hook's regex would leave the suite green today.

Dismissed after verification (not reported): the `norm("")` → `"."` behavior change (inert — `parts()` filters `.`); linker-rule false positives on `cp -s` *from* disputes (documented conservative refusal); cwd-relative evasions of bash_guard (pre-existing, out of this diff's scope, and ledger edits are caught by the pre-push append-only gate).

Verdict: **approve** — no critical or major findings.

```json
{"verdict": "approve", "findings": [{"severity": "minor", "file": "tests/test_trust_hooks.py", "line": 590, "issue": "The round-4 MAJOR fix (reviews/disputes/ added to the never-inert list in all three hooks) and the row-111 linker spellings (ln -s --target-directory=, cp -s) shipped without regression tests — only the :(glob) hash fix got one — despite WT.12 item 5, codified in this same commit, requiring plant-the-defect tests.", "scenario": "A later change 'simplifies' one hook's instruction-bearing regex back to '^(\\.claude/|\\.githooks/|\\.github/|scripts/)|^CLAUDE\\.md$' (e.g. copy-pasted from an older plan revision); the full suite stays green because no test commits a disputes-only branch and asserts the docs-only exemption is denied, and disputes-only branches silently regain the skip-review path to main — the exact end state round 4 rated major."}]}
```
