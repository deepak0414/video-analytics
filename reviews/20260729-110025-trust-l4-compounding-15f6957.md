# Agent review — request_changes

date: 2026-07-29T11:03:39.433117
range: origin/main..HEAD
branch: trust/l4-compounding
findings: 2

- **major** `workflow-trust-plan.md:2011` — Over-broad find-replace during the descope replaced four still-valid citations — (row 100), (row 102), (row 103), (row 104) in the pre-existing PR-4 label-guard narrative — with '(see the deferred-hook note in WT.8)', even though those matrix rows still exist unchanged at lines 2420-2424; the historical record now points at an unrelated note (also lines 2022, 2024, 2026).
  - scenario: A reader auditing the label-guard behavior follows the PR-4 narrative's citation for 'gh pr create --body blocked' and lands on the deferred self-observing-loop-hook note instead of matrix row 100, which still documents the actual test; the plan's provenance chain for four gate behaviors is severed while the targets sit intact in the same file.
- **minor** `tests/test_lessons_section.py:31` — entries() only recognizes bullets matching '- YYYY-MM-DD: ', so an undated or malformed lesson bullet is invisible to every enforcement test — budget, length, date, and dedup all skip it, and nothing asserts all bullets conform.
  - scenario: A hand-edited or misformatted /lesson entry like '- always check fps first' is appended; the section grows past 20 entries but test_section_within_pruning_budget stays green because the non-conforming bullets are never counted, silently defeating the mechanical pruning rule this file exists to enforce.

---

## Full review

Review complete. This commit's deliverable (the `/lesson` command, the CLAUDE.md Lessons section, and `tests/test_lessons_section.py`) is sound: the test parser correctly joins wrapped entries before measuring (fixing the round-2 unenforceability defect), the WT.8 "Done when" is met, no tests were deleted or weakened, and the descope of the loop hook is honestly recorded. I found one major defect and one minor, both verified.

**Major — over-broad find-replace corrupted four valid cross-references** (`workflow-trust-plan.md:2011`, 2022, 2024, 2026). The descope edit that replaced references to removed matrix rows 106–124 with "(see the deferred-hook note in WT.8)" also clobbered `(row 100)`, `(row 102)`, `(row 103)`, and `(row 104)` in the *pre-existing PR-4 label-guard narrative*. I verified those four rows still exist, unchanged, in the §8 matrix on both origin/main and HEAD (`workflow-trust-plan.md:2420-2424`), and origin/main's prose cited them correctly. The result is garbled history: e.g. "the guard blocked the very lifecycle step that opens a PR (see the deferred-hook note in WT.8)" now sends a reader from a label-guard incident to an unrelated note about a deferred loop hook, while the correct citation target sits intact 400 lines below. Neighboring references (rows 99, 101, 105) were left alone, confirming this was an untargeted substitution, not a renumbering. This is the exact defect class the diff's own Lessons list warns about (over-broad matching), and the plan's own review history treats documentation asserting something false as a blocking finding.

**Minor — the enforcement tests silently ignore non-conforming entries** (`tests/test_lessons_section.py:31`). `entries()` only recognizes bullets matching `- YYYY-MM-DD: `; an undated bullet (e.g. `- always check fps first`) is invisible to every check — it escapes the 20-entry budget, the 400-char limit, the date check, and the dedup check, and no test fails on its presence. Scenario: a hand-edit or a `/lesson` run that drops the date grows the section unboundedly while the suite that exists to make the pruning rule mechanical stays green. A cheap fix is asserting that every `- ` bullet after the first dated entry matches the entry format.

I checked the dispute channel: no `reviews/` ledger disputes either finding (the round-4 dispute in the plan concerns the removed loop hook, not these). The working-tree modifications to `registry.py`, `schema.py`, and `test_trace_link.py` are uncommitted and outside the reviewed range.

```json
{"verdict": "request_changes", "findings": [{"severity": "major", "file": "workflow-trust-plan.md", "line": 2011, "issue": "Over-broad find-replace during the descope replaced four still-valid citations — (row 100), (row 102), (row 103), (row 104) in the pre-existing PR-4 label-guard narrative — with '(see the deferred-hook note in WT.8)', even though those matrix rows still exist unchanged at lines 2420-2424; the historical record now points at an unrelated note (also lines 2022, 2024, 2026).", "scenario": "A reader auditing the label-guard behavior follows the PR-4 narrative's citation for 'gh pr create --body blocked' and lands on the deferred self-observing-loop-hook note instead of matrix row 100, which still documents the actual test; the plan's provenance chain for four gate behaviors is severed while the targets sit intact in the same file."}, {"severity": "minor", "file": "tests/test_lessons_section.py", "line": 31, "issue": "entries() only recognizes bullets matching '- YYYY-MM-DD: ', so an undated or malformed lesson bullet is invisible to every enforcement test — budget, length, date, and dedup all skip it, and nothing asserts all bullets conform.", "scenario": "A hand-edited or misformatted /lesson entry like '- always check fps first' is appended; the section grows past 20 entries but test_section_within_pruning_budget stays green because the non-conforming bullets are never counted, silently defeating the mechanical pruning rule this file exists to enforce."}]}
```
